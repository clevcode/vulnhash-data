# Anchor batches

VulnHash creates one immutable `batches/<merkle-root>.json` file after the
matching Base transaction has succeeded and reached the configured
confirmation threshold.

Files in this directory are factual evidence data offered under CC0-1.0. The
directory README and repository tooling remain MIT-licensed.
